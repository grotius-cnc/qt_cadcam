#ifndef COPY_MIRROR_ARRAY_H
#define COPY_MIRROR_ARRAY_H

#include <QMainWindow>

class copy_mirror_array
{
public:
    copy_mirror_array(int in, int out, double p1x_mirror, double p1y_mirror, double p2x_mirror, double p2y_mirror, double angle);
};

#endif // COPY_MIRROR_ARRAY_H

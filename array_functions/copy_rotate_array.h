#ifndef COPY_ROTATE_ARRAY_H
#define COPY_ROTATE_ARRAY_H

#include <QMainWindow>

class copy_rotate_array
{
public:
    copy_rotate_array(int in, int out, double rotate_degrees, double rotate_origin_x, double rotate_origin_y);
};

#endif // COPY_ROTATE_ARRAY_H

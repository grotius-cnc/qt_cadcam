#ifndef STRETCH_ARC_ARRAY_H
#define STRETCH_ARC_ARRAY_H

#include <QMainWindow>

class stretch_arc_array
{
public:
    stretch_arc_array(int i, bool startpoint, bool midpoint, bool endpoint, double dist_x, double dist_y, double dist_z);
};

#endif // STRETCH_ARC_ARRAY_H
